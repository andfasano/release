workflow:
  as: baremetal-ds
  steps:
    pre:
    - ref: packet-setup
    test:
    - ref: baremetal-ds-test
    post:
    - ref: packet-teardown
  documentation: |-
    Dev-scripts workflow on baremetal
